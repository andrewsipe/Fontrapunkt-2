/**
 * Toasts in this app use react-hot-toast (https://react-hot-toast.com/).
 *
 * - Add <Toaster /> in AppProviders (already present).
 * - Call toast("message"), toast.success(), toast.error(), or toast(<JSX />) from anywhere.
 * - Use shared.standardToast for token-based container styling (see ExportButtons, EmptyState).
 *
 * This file no longer provides a Base UI Toast component.
 */

export {};
